'use strict';

$('.owl-carousel').owlCarousel({
	items:6,
	loop:true,
	autoplayTimeout:7000,
	autoplay: true,
	autoplaySpeed: 500,
})

/*$('.owl-carousel').owlCarousel({
    animateOut: 'slideOutDown',
    animateIn: 'flipInX',
    items:1,
    margin:30,
    stagePadding:30,
    smartSpeed:450
});*/